return {
  close_if_last_window = true,
  enable_diagnostics = true,
  enable_git_status = true,
  window = {
    position = "right",
    width = 25,
    mappings = {
      ["o"] = "open",
    },
  },
}
